ServerRoot "/usr/local/apache2"
Listen 80

# Core bits
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so

# Proxy
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

User daemon
Group daemon

ServerName localhost
ErrorLog /proc/self/fd/2
CustomLog /proc/self/fd/1 combined

DocumentRoot "/usr/local/apache2/htdocs"
<Directory "/usr/local/apache2/htdocs">
  Require all granted
  Options -Indexes
</Directory>

# Serve index.html at /
<IfModule dir_module>
  DirectoryIndex index.html
</IfModule>

# Proxy only API paths to the backend container "myapp" on port 8080
# Adjust paths as needed (e.g. /api/ or /students)
<VirtualHost *:80>
  ProxyPreserveHost On

  # Example: proxy everything under /students to the backend
  ProxyPass        /students http://myapp:8080/students
  ProxyPassReverse /students http://myapp:8080/students

  # Or, if you have multiple endpoints, proxy a prefix:
  # ProxyPass        /api/ http://myapp:8080/
  # ProxyPassReverse /api/ http://myapp:8080/
</VirtualHost>
